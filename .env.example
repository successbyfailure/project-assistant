# Fulcrum Environment Configuration
# Usage: Run './generate_secrets.sh' to automatically populate this file into a '.env' with random keys.

# JWT Secret for session tokens
JWT_SECRET=your_super_secret_key_here

# PostgreSQL Configuration
DB_PASSWORD=postgres
DATABASE_URL=postgresql+asyncpg://postgres:${DB_PASSWORD}@db:5432/fulcrum

# GitHub Integration (Phase 2)
# GITHUB_CLIENT_ID=your_github_client_id
# GITHUB_CLIENT_SECRET=your_github_client_secret

# Coder OAuth (MCP Bridge)
# CODER_OAUTH_CLIENT_ID=your_coder_oauth_client_id
# CODER_OAUTH_CLIENT_SECRET=your_coder_oauth_client_secret
# CODER_OAUTH_SCOPE=
CODER_BASE_URL=https://coder.example.com

# Base domain for OAuth callbacks
BASE_DOMAIN=http://localhost:8000

# Admin Configuration
# FULCRUM_ADMIN_EMAIL=admin@example.com
